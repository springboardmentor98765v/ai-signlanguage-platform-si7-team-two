# ============================================================
# Placeholder ONLY — no real database exists yet (that's Day 2,
# see SRS §6 Intern 5 Day 2). These are the variable NAMES the
# team is contracting on now so Intern 2 (Backend) and Intern 4
# (Business Logic) can write config-loading code today without
# being blocked, and swap in real values once Day 2 stands up
# the actual Postgres instance.
# ============================================================

# --- Database (Data Layer) ---
DB_ENGINE=postgresql
DB_HOST=localhost
DB_PORT=5432
DB_NAME=signlang_platform
DB_USER=signlang_app
DB_PASSWORD=your_secure_password_here
DATABASE_URL=postgresql://signlang_app:your_secure_password_here@localhost:5432/signlang_platform

# --- Auth (contract with Intern 2, needed by Day 4) ---
JWT_SECRET_KEY=changeme_generate_a_real_secret
JWT_ALGORITHM=HS256
JWT_EXPIRY_MINUTES=60

# --- AI Service (contract with Intern 3, needed by Day 6) ---
AI_SERVICE_URL=http://localhost:8001

# --- Backend API (contract with Intern 2) ---
BACKEND_API_URL=http://localhost:8000

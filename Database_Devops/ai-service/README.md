# AI/ML & Computer Vision Service (Intern 3)

Placeholder — pipeline work begins Day 2. Prediction output contract
(`predicted_sign`, `confidence`) must match the `assessments` table columns
defined in `db/schema/schema.sql`.

# PLACEHOLDER — replace this file with Koushik's actual ai-service
# requirements.txt (from his AIML/CV folder) before building the Docker
# image. Listed below are the packages implied by the shared model
# artifacts (xgboost model + label encoder) in case his file needs merging
# with whatever the Milestone 1 AI service already had (opencv, mediapipe,
# fastapi, etc.) rather than a straight replace.

fastapi
uvicorn[standard]
opencv-python-headless
mediapipe
numpy
xgboost
scikit-learn   # needed to unpickle the LabelEncoder
joblib

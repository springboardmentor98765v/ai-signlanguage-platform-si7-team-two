fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0

# Intern 3 will add real CV/ML dependencies here once the placeholder
# /predict endpoint is replaced with the real pipeline, e.g.:
# mediapipe>=0.10,<1.0
# opencv-python-headless>=4.9,<5.0
# numpy>=1.26,<2.0
